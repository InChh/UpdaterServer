# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: updater-server
services:  
  app:
    language: dotnet
    project: ./src/UpdaterServer.AppHost/UpdaterServer.AppHost.csproj
    host: containerapp
